import 'package:flutter/material.dart';

class LCachedNetworkImage extends StatelessWidget {
  const LCachedNetworkImage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
